import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.petfriends.petfriends',
  appName: 'Petfriends',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
